[pytest]
testpaths = tests
pythonpath = . tests/fixtures
addopts = --strict-markers -m "not live"
markers =
    live: makes a real network call to the translation API (run explicitly with: pytest -m live)
